"""Settings package.

The active settings module is chosen by the DJANGO_SETTINGS_MODULE environment
variable (e.g. ``itrix.settings.development`` or ``itrix.settings.production``).
``base.py`` holds everything shared; the environment modules override it.
"""

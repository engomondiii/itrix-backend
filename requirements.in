# itriX backend — dependencies (unpinned source list)
Django
djangorestframework
djangorestframework-simplejwt
django-cors-headers
django-filter
python-dotenv
dj-database-url
psycopg[binary]
whitenoise
gunicorn
celery
redis
channels
channels-redis
daphne
anthropic
openai
pinecone
resend
python-docx
pypdf
# Attachment extraction: the xlsx and pptx handlers have existed since v6.0 and
# degraded to metadata-only because neither library was ever installed, so a
# spreadsheet or a deck attached to a conversation was accepted and unread.
openpyxl
python-pptx
boto3
requests
python-slugify
cbor2==5.6.5
